{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"],
  // Self-contained knip drift-probe fixture, excluded the same way it's
  // excluded from knip.json/eslint.config.js/.fallowrc.jsonc — keep the four
  // exclusions in sync.
  "exclude": ["test/drift/knip-fixture/**"]
}
